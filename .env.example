# Gemini API Key (Required for AI features)
API_KEY=
GEMINI_API_KEY=

# Firebase Configuration (Required for Cloud Sync & Public Interviews)
# You can set these up using the "Set up Firebase" tool in AI Studio
# or by providing a firebase-applet-config.json file.

# Google Analytics (Optional)
# Your Measurement ID (e.g., G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=
